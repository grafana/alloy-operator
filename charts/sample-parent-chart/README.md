# Sample Parent Chart

This chart is used only for CI testing of the Alloy Operator as an example of embedding the operator chart into
another Helm chart. We then utilize this chart in integration tests.
